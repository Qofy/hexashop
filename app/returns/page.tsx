import ReturnsClient from '@/component/returns/ReturnsClient';

export const metadata = {
  title: 'Returns & Exchanges | Hexashop',
  description: 'Easy returns and exchanges process for all products',
};

export default function ReturnsPage() {
  return <ReturnsClient />;
}
